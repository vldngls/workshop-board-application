PORT=4000
HOST=0.0.0.0
# For single-machine setups you can leave HOST unset or change to 127.0.0.1
# For LAN access keep HOST=0.0.0.0 and set WEB_ORIGIN to your workstation IP
MONGODB_URI=mongodb://localhost:27017/workshop-board
JWT_SECRET=replace-with-strong-secret
# API Key for validating against external API key manager service
# Get your API key from https://api-key-manager-one.vercel.app/
# REQUIRED: API key validation is enabled by default
# If missing or invalid, application will be in maintenance mode
API_KEY=
# Optional: Override the API key validation URL (defaults to https://api-key-manager-one.vercel.app/api/validate)
API_KEY_VALIDATOR_URL=https://api-key-manager-one.vercel.app/api/validate
APP_URL=http://localhost:3000
# When sharing on the network update to http://<your-pc-ip>:3000
WEB_ORIGIN=http://localhost:3000
# When sharing on the network update to http://<your-pc-ip>:3000
NODE_ENV=development
